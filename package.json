{
  "name": "pierat",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "bot",
    "shared",
    "webapp"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:bot": "npm run dev -w bot",
    "dev:web": "npm run dev -w webapp",
    "build:shared": "npm run build -w shared",
    "deploy:bot": "npm run deploy-commands -w bot",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
